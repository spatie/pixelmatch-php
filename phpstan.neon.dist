includes:
    - phpstan-baseline.neon

parameters:
    paths:
        - src/

    # Level 9 is the highest level
    level: 8

    excludePaths:

    ignoreErrors:
        - '#Unsafe usage of new static#'
